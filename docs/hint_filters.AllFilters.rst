hint_filters.AllFilters module
==============================

The AllFilters module defines the list of possible filters that can be
used in the (no longer used) hint filter system. It just imports them
and puts them in a list to be used later.

.. automodule:: hint_filters.AllFilters
    :members:
    :undoc-members:
    :show-inheritance:
